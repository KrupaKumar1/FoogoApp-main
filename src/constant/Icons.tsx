// GlobalIcons.js

import React from 'react';
import * as Icons from 'react-native-vector-icons';

// Export all the icons as named exports
export const FontAwesome = Icons.FontAwesome;
export const Entypo = Icons.Entypo;
// Add other icon sets here as needed

// You can define more icon sets similarly if needed

const GlobalIcons = () => {
  // This component doesn't render anything, it just serves as a container for exporting icons
  return null;
};

export default GlobalIcons;
